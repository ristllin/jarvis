# Copy to ngrok-policy.yml and set your credentials.
# Or use ngrok-policy.yml directly (add to .gitignore).
on_http_request:
  - actions:
      - type: basic-auth
        config:
          credentials:
            - ristlin:your-strong-password
